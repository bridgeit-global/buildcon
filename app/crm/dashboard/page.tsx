import { ModulePlaceholder } from '../_components/module-placeholder';

export default function DashboardPage() {
  return (
    <ModulePlaceholder
      title="Dashboard"
      description="KPI cards + charts (inventory status, collections, sales vs collections)."
    />
  );
}

