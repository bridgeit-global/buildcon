import { PortalPlaceholder } from '../_components/portal-placeholder';

export default function PortalServicePage() {
  return (
    <PortalPlaceholder
      title="Service requests"
      description="Log snags, possession queries, and maintenance requests. A ticketing workflow will connect this page to your project team."
    />
  );
}
