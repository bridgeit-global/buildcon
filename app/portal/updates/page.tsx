import { PortalPlaceholder } from '../_components/portal-placeholder';

export default function PortalUpdatesPage() {
  return (
    <PortalPlaceholder
      title="Construction updates"
      description="Milestone and construction progress for your project will be summarised here (linked to CLD and site updates from the CRM)."
    />
  );
}
